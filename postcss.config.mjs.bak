// postcss.config.mjs
import postcss from '@tailwindcss/postcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: {
    [postcss]: {},  // v4 plugin (note the array syntax for ES modules)
    autoprefixer,
  },
};
